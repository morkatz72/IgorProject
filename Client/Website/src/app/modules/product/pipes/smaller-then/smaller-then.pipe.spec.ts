import { SmallerThenPipe } from './smaller-then.pipe';

describe('SmallerThenPipe', () => {
  it('create an instance', () => {
    const pipe = new SmallerThenPipe();
    expect(pipe).toBeTruthy();
  });
});
