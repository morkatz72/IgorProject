import { BiggerThenPipe } from './bigger-then.pipe';

describe('BiggerThenPipe', () => {
  it('create an instance', () => {
    const pipe = new BiggerThenPipe();
    expect(pipe).toBeTruthy();
  });
});
